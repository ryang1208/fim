<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.quantdo.{projectName}.mapper.{clsname}Mapper">
	
	<select id="backPaging{clsname}" parameterType="map" resultType="{entity}">
	 <!-- sql -->
	</select>

</mapper>